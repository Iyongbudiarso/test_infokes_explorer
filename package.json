{
  "name": "infokes-explorer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && bun run dev\" \"cd frontend && bun run dev\"",
    "install:all": "concurrently \"cd backend && bun install\" \"cd frontend && bun install\"",
    "seed": "cd backend && bun run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
